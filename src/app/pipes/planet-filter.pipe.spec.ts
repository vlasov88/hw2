import { PlanetFilterPipe } from './planet-filter.pipe';

describe('PlanetFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PlanetFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
